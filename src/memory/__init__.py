"""Memory: conversation state and context persistence (Postgres/Redis).

`memory` may depend on `core`, `models`, and `observability` only. No AI
functionality is implemented yet — this package currently defines the
architectural seam.
"""
