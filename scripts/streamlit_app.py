"""Streamlit UI entrypoint (placeholder).

Run locally with:

    streamlit run scripts/streamlit_app.py

No chat UI or AI functionality is implemented yet — this is the foundation
commit only. Real UI work lands in a later milestone.
"""

from __future__ import annotations

import streamlit as st

st.set_page_config(page_title="Enterprise AI Assistant (POC)")
st.title("Enterprise AI Assistant")
st.info("Foundation build — UI functionality is not implemented yet.")
